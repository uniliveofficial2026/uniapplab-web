# Rigs

`MASTER.blend` currently includes a first-pass `UniLivesRig` humanoid armature with automatic weights.

Next artist pass in Blender:
1. Align bone joints to anatomy
2. Re-bind / weight-paint problem areas
3. Add face bones or refine shape keys for expressions + lip-sync
4. Re-export with `tools/character-pipeline/blender/export-optimized-glb.py`
