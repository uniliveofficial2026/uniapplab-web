# Live UI

Solo Live footer uses `--app-composer-bottom-inset`. Chat input `data-testid="live-chat-input"`, font-size 16px (anti-zoom).

RTC session must not remount on viewport updates (layout-only CSS vars).

Panels (Gift/Sticker/Beauty/Voice/Games/Guest/PK/More/Shop): geometry audit pending physical.
