# Phase 3 authentication screenshots / previews

Official brand: **UniLive’s**

Local review only — not uploaded.

Production auth artwork is **missing**, so previews use `/brand/app-logo.png` fallback + UniLive’s auth token styling.

Files are named: `{mode}-{viewport}-{motion}.html`
