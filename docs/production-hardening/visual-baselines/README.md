# Pixel visual baselines (pointer)

Committed Stage A screenshot baselines live in the app package:

**`artifacts/instacollab/test/visual-baselines/`**

Commands (from `artifacts/instacollab`):

- `pnpm run visual:capture-baselines`
- `pnpm run test:visual-pixel`
- `pnpm run smoke:messages-mount`
- `pnpm run smoke:marketplace-mount`

uiUxChanged: false. Stage B locked.
