/**
 * Barrel for non-visual app providers.
 * Visual layout / theme providers stay in contexts/ and lib/ — do not relocate UI here.
 */
export { AppQueryProvider, getAppQueryClient } from './AppQueryProvider';
