/** LiveKit publish resolution when beauty/AR is off (lightweight raw lane). */
export const LIVE_VIDEO_WIDTH = 480;
export const LIVE_VIDEO_HEIGHT = 360;

export {
  WEBAR_CAMERA_WIDTH,
  WEBAR_CAMERA_HEIGHT,
  WEBAR_CAMERA_FPS,
  WEBAR_CAMERA_IDEAL,
  WEBAR_CAMERA_FRAME_RATE,
  WEBAR_OUTPUT_FPS,
} from '../../lib/webar/webarCameraConfig';
