# @unilives/cloud

UniLive Cloud control plane MVP: organizations, projects, environments, RBAC, secretRefs, provider connections, deployments, usage, quotas, rate limits, audit.

Never stores or returns plaintext secrets. Production media provider remains LiveKit via UniLiveRTC.
