# Placeholder LiveKit config for self-host reference.
# Replace keys before production — values are NOT production secrets.
port: 7880
rtc:
  tcp_port: 7881
  port_range_start: 50000
  port_range_end: 60000
keys:
  devkey: CHANGE_ME_LIVEKIT_SECRET
