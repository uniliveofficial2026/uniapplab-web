- [Repost feature & faithful-port directive](repost-faithful-port.md) — repost uses embedded `repost?: Post` model; user wants their EXACT source code, do not "improve" it.
- [Account switcher (remix_-instacollab)](account-switcher-remix-port.md) — Add/Switch Account UI + device `user_accounts`; reference zip at `attached_assets/extracted/remix_-instacollab/`.
- **Karaoke (remix_-instacollab-new.zip)** — verbatim port of `src/components/karaoke/*` (6 files: `KaraokeScreen`, `RecordingStudio`, `ChallengeView`, `LeaderboardView`, `SingingPlayerModal`, `SongUpload`). Source zip: `/Users/wei/Downloads/remix_-instacollab-new.zip`. Shell integration: `karaoke` nav after Reels with `Mic` icon; hide desktop sidebar, mobile top/bottom nav when `currentTab === 'karaoke'`; full-height overflow-hidden main layout (same as messages). Do NOT edit karaoke component files unless user asks.
- **Rooms (smule-clone.zip)** — verbatim port under `src/smule-rooms/` (pages: `Party`, `Room`, `CreateRoom`, `EditRoom`, `RoomDetails`; components: `Header`, `Navigation`, `DesktopNavigation`, `SongSelector`, `LyricsOverlay`, `GuestManagementOverlay`, `RoomViewersOverlay`, `ArenaRankingsOverlay`, `WatchTogetherView`; utils: `storage.ts`; styles: `smule-rooms.css`). Source zip: `/Users/wei/Downloads/smule-clone.zip`. `RoomsHost.tsx` mirrors smule `App.tsx` layout shell (desktop sidebar + mobile bottom nav + route chrome) — only integration file, not from zip. Shell: `rooms` tab labeled **Party** with `Sparkles` icon; InstaCollab chrome hidden on that tab. Create Room via Party → "Start Room". Do NOT edit smule-rooms page/component files unless user asks.
